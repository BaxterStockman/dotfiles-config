#!/bin/sh

alsa_conky_main() {
    local sink="${1:-Master}"

    local vol=''
    if vol="$(amixer get "$sink" | grep Mono: | awk '{print $4}' | sed 's/\[\([0-9]\{,2\}\).\]/\1/g')"; then
        echo "$vol"
    fi
}

alsa_conky_main "$@"
